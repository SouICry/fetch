//Fetches the 3 pages of tickets (history, yourDelivery, queue, called when loading these pages),
//stores into front end loader. Returns data in format these 3 pages need



//Then when ticket is selected (by array index value - store it as a data attribute), loads that ticket into
//loader.currentTicket



//Then for pages that need the ticket data, convert the ticket into needed format:
//viewTicket, driverList, confirmCompletion, rateUser
//shoppingStatus, confirmTicket, rateDriver